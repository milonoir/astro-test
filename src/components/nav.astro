---
const { items } = Astro.props;
---

<nav>
    <ul>
        {items.map(({ text, href }) => (
            <li>
                <a href={href}>{text}</a>
            </li>
        ))}
    </ul>
</nav>
